# kendoui-realtime-chart
Realtime Data Visualization dashboard with Socket.io, kendoUI and jquery

* To Install navigate into any of the folder and type `npm install` in your terminal.
* Type `node start` in your terminal and visit: http://localhost:3000 to see your app.
